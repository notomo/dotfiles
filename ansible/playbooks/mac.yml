- hosts: 'local'
  roles:
    - 'mac'
    - 'dotfiles'
    - 'tools'
    - 'php'
    - 'node'
    - 'docker'
    - 'neovim'
  vars_files:
    - '{{ inventory_dir | dirname }}/vars/{{ ansible_os_family }}.yml'
