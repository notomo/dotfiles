- hosts: 'local'
  roles:
    - 'workspace'
    - 'bash'
    - 'golang'
    - 'dotfiles'
    - 'tools'
    - 'python'
    - 'php'
    - 'node'
    - 'docker'
    - 'neovim'
    - 'git'
  vars_files:
    - '{{ inventory_dir | dirname }}/vars/{{ ansible_os_family }}.yml'
