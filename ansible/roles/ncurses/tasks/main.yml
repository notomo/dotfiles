- name: install dependencies with yum
  yum:
    name:
      - ncurses-devel
  when: is_centos
  become: yes

- name: install dependencies with apt
  apt:
    name:
      - libncurses-dev
  when: is_ubuntu
  become: yes
