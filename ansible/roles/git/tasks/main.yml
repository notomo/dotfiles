
- name: install git with homebrew
  homebrew: name=git
  when: is_macos

- name: install yum repository for git
  yum: name=http://rpms.famillecollet.com/enterprise/remi-release-7.rpm
  when: is_centos
  become: yes

- name: install git with yum
  yum: name=git
  when: is_centos
  become: yes
