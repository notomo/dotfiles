
- homebrew:
    name:
      - python3

- name: set variables
  set_fact:
    python3_packages:
      - isort
      - black
      - python-lsp-server[all]
      - httpie
      - pytest

- pip:
    name: "{{ python3_packages }}"
    executable: pip3
