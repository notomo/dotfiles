- name: install with homebrew
  homebrew:
    name:
      - luarocks
      - lua@5.1
      - lua-language-server
      - stylua

- name: install vusted
  shell: luarocks --lua-version=5.1 install vusted
